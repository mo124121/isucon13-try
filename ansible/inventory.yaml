all:
  vars:
    hosts_ips:
      S1: 52.192.73.102
      S2: 43.206.213.137
      S3: 57.180.245.77
      B: 52.195.233.170

  children:
    target:
      children:
        nginx:
          hosts:
            nginx01:
              ansible_host: "{{ hosts_ips.S1 }}"
        webapp:
          hosts:
            webapp01:
              ansible_host: "{{ hosts_ips.S1 }}"
        db_web:
          hosts:
            db_web01:
              ansible_host: "{{ hosts_ips.S3 }}"
        pdns:
          hosts:
            pdns01:
              ansible_host: "{{ hosts_ips.S2 }}"
        db_pdns:
          hosts:
            db_pdns01:
              ansible_host: "{{ hosts_ips.S2 }}"
    outsider:
      hosts:
        benchmarker:
          ansible_host: "{{ hosts_ips.B }}"
